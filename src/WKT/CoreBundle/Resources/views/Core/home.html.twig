{# src/WKT/CoreBundle/Resources/views/Core/home.html.twig #}
{% extends 'WKTCoreBundle:Core:layout.html.twig' %}
{% block body %}
    <div class="jumbotron">
    	<h1>WikiTech</h1> 
    	<p>Le numérique pour tous, par tous 😁</p>
    </div>
    <hr>
    {% if app.user and trainings is not empty %}
        <div class="panel panel-success">
            <div class="panel-heading"><h2>Continuez vos formations</h2></div>
            <div class="panel-body">
                {% for training in trainings %}
                    <div class="col-sm-6 col-md-4">
                        <div class="thumbnail">
                            {% if training.image is empty %}
                                <img src="{{asset('images/training.jpeg')}}" alt="Miniature Wikitech">
                            {% else %}
                                <img src="{{ asset(training.image.webPath) }}" alt="{{ training.image.alt }}">
                            {% endif %}

                            <div class="caption">
                            {% if trainingsBeginned[training.title] is defined %}
                                {% if trainingsBeginned[training.title].finished %}
                                    <h3 class=""> {{ training.title }}</h3> <span class="label label-danger">Terminée</span>
                                    <hr>
                                    <p> {{ training.homeText }} </p>
                                    <hr>
                                    <p class="text-center"><a class="btn btn-primary" href="{{ path('wkt_platform_view', { 'id': training.id, 'slugTraining': training.slug}) }}">Voir la formation</a></p>
                                {% elseif trainingsBeginned[training.title].updated %}
                                    <h3 class=""> {{ training.title }}</h3> <span class="label label-warning">Modifications</span>
                                    <hr>
                                    <p> {{ training.homeText }} </p>
                                    <hr>
                                    <p class="text-center"><a class="btn btn-success" href="{{ path('wkt_platform_article_view', { 'id': trainingsBeginned[training.title].article.id, 'slugTraining': training.slug, 'slugArticle': trainingsBeginned[training.title].article.slug}) }}">Continuer la formation</a></p>
                                {% else %}
                                    <h3 class=""> {{ training.title }}</h3> <span class="label label-success">En cours</span>
                                    <hr>
                                    <p> {{ training.homeText }} </p>
                                    <hr>
                                    <p class="text-center"><a class="btn btn-success" href="{{ path('wkt_platform_article_view', { 'id': trainingsBeginned[training.title].article.id, 'slugTraining': training.slug, 'slugArticle': trainingsBeginned[training.title].article.slug}) }}">Continuer la formation</a></p>
                                {% endif %}
                            {% else %}
                                <h3 class=""> {{ training.title }}</h3>
                                <br>
                                <hr>
                                <p> {{ training.homeText }} </p>
                                <hr>
                                <p class="text-center"><a class="btn btn-primary" href="{{ path('wkt_platform_view', { 'id': training.id, 'slugTraining': training.slug}) }}">Commencer la formation</a></p>
                            {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
	{% else %}
    	<div class="panel panel-default">
    		<div class="panel-heading"><h2>Commencer une formation</h2></div>
    		<div class="panel-body">
	    	    {% for training in trainings %}
    	        <div class="col-sm-6 col-md-4">
                    <div class="thumbnail">
                        {% if training.image is empty %}
                             <img src="{{asset('images/training.jpeg')}}" alt="Miniature Wikitech">
                        {% else %}
                          <img src="{{ asset(training.image.webPath) }}" alt="{{ training.image.alt }}">
                        {% endif %}

                        <div class="caption">
                            <h3 class=""> {{ training.title }}</h3>
                            <hr>
                            <p>
                                {{ training.homeText }} 
                            </p>
                            <hr>
                            <p class="text-center"><a class="btn btn-primary" href="{{ path('wkt_platform_view', { 'id': training.id, 'slugTraining': training.slug}) }}">Voir la formation</a></p>
            	        	<p class="text-center"><small><em>Crée le {{ training.createdAt|date('d/m/Y') }}</em></small></p>
                        </div>
                    </div>
    	        </div>
    	    {% endfor %} 
			</div>
		</div>
    {% endif %}
    <hr>
    {% if app.user %}
    {% else %}
        <div class="panel panel-info">
            <div class="panel-heading"><h2>Créer un profil</h2></div>
            <div class="panel-body">
                <p>
                    Créer un profil vous donne accès à énormément d’avantages. Vous pourriez par exemple savoir quels sont les cours que vous avez suivi ou cummuler des points de contributions pour vos apports.
                </p>
                <a class="btn btn-info" href="{{ path('fos_user_registration_register') }}">S'inscrire</a> 
                <a class="btn btn-default" href="{{ path('fos_user_security_login') }}">Se connecter</a>         
            </div>
        </div>
    <hr>
    {% endif %}
    <div class="panel panel-danger">
        <div class="panel-heading"><h2>Soutenir le projet</h2></div>
        <div class="panel-body">
            <p>
                La collaboration est au cœur du projet Wikitech. De la rédaction de cours à un simple partage, de nombreux moyens d’apporter sa contribution existent quelque soit votre degrés d’expertise informatique et de disponibilité.
            </p>
            <a class="btn btn-danger" href="#">En savoir plus</a>          
        </div>
    </div>
    <hr>
    <div class="panel panel-warning">
        <div class="panel-heading"><h2>Top Contributeurs <a href="{{path('wkt_core_leaderboard')}}"><span class="label label-warning">Voir tout</span></a></h2></div>
        <div class="panel-body">
            {% if users is empty %}
                <p class="text-center"><small><em>Aucun utilisateur enregistré</em></small></p>
            {% else %}
                <table class="table">
                    <thead>
                        <tr>
                            <th>Rang</th>
                            <th>Utilisateur</th>
                            <th>Score</th>
                            <th>Profil</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for user in users %}
                            <tr>
                                <td>{{loop.index}}</td>
                                <td>{{user.firstname}} {{user.lastname}}</td>
                                <td>{{user.nbPoint}} pts</td>
                                <td><a class="btn btn-warning btn-xs" href="{{path('wkt_user_view_profile', {'id': user.id})}}">Profil</a></td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>                    
            {% endif %}
        </div>
    </div>
    <hr>
    <div class="panel panel-primary">
        <div class="panel-heading"><h2>Donnez-nous votre avis</h2></div>
        <div class="panel-body">
            <p>
                Créer un profil vous donne accès à énormément d’avantages. Vous pourriez par exemple savoir quels sont les cours que vous avez suivi ou cummuler des points de contributions pour vos apports.
            </p>
            <a class="btn btn-primary" href="{{path('wkt_core_contact_send_email')}}">Faire un retour</a>          
        </div>
    </div>
{% endblock %}
